node_modules/
dist/
dist-electron/
.vite/
.env
*.log
__pycache__/
.venv/
.venv-worker/
.cache/

# Self-built sherpa-onnx arm64 wheel for CPU diarization: not on PyPI, too large
# to commit. Built locally / fetched from a GitHub release; supplied to
# package-windows-v1.ps1 via -SherpaWheel or MUESLI_SHERPA_WHEEL. Kept out of git.
worker/wheels/
bin/
obj/
artifacts/
publish/

# Agent context / handoffs
Context/

# IDE
.vs/
*.user
*.suo
.idea/
.vscode/

# Temp/backup
*.tmp
*.bak

# Legacy Electron/web stack (not used — WPF is the active app)
shared/
src/
electron/
index.html
vite.config.ts
tsconfig.json
tsconfig.node.json
package.json
package-lock.json
